spring:
  datasource:
    url: jdbc:mysql://localhost:32768/rentDB?createDatabaseIfNotExist=true
    username: root
    # password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
